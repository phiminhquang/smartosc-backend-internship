spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rbac_management
    username: ex
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

jwt:
  signer-key: ${JWT_SIGNER_KEY}
  expiration-seconds: 3600

app:
  admin:
    email: admin@gmail.com
    password: ${ADMIN_PASSWORD}
    name: Administrator
