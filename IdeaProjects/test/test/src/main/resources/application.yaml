spring:
  datasource:
    url: jdbc:mysql://localhost:3306/student_management_practice
    username: test
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
